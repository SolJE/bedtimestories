# 2026-07-24

## 修复 story-021 页面空白问题
- **根因**: story-021.json 使用了 `content` 结构（`type`/`speaker`），但 `story.js` 只识别 `paragraphs` 结构（`id`/`speaker`/`speakerZh`/`text`）
- **修复**: 将 `content` 数组转换为 `paragraphs` 数组，为每段添加 `id`（p1-p38）、`speakerZh`；将 `illustrations` 字符串数组改为 `{paragraphId, image, alt}` 对象数组，分别映射到 p3、p12、p33
- **提交**: 15858b0 - 已推送 ✅
